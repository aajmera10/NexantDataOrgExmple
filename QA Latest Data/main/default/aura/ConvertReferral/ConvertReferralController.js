({
	doInit : function (component, event, helper) {
		helper.prepareStatusInformation(component);
	},

	startConvertation : function (component, event, helper) {
		helper.startConvertation(component);
	},
})