({

    selectTradeAllyDuplicate: function(component, event, helper) {
    	helper.selectTradeAllyDuplicate(component, event);
    },

    declineAllTradeAllyDuplicates: function(component, event, helper) {
    	helper.declineAllTradeAllyDuplicates(component, event);
    }

})